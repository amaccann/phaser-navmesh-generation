export default class Portal {
  /**
   * @constructor
   * @param {Phaser.Point} left
   * @param {Phaser.Point} right
   */
  constructor(left, right) {
    this.left = left;
    this.right = right;
  }
}
